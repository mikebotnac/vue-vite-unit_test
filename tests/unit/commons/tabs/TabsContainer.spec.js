import { mount } from '@vue/test-utils';
import TabsContainer from '@/components/commons/tabs/TabsContainer.vue';


describe('TabsContainer', () => {
  it('should display proper render', () => {
    // const wrapper = mount(TabsContainer, { props: { label: 'Password' } })
    // const labelText = wrapper.find('label');
    // expect(labelText.text()).toContain('Password')
    expect(true).toBe(true)
  })
})

