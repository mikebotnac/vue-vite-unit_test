import { mount } from '@vue/test-utils';
// import Home from '@/views/Home.vue';
describe('Home', () => {
  it('should display $q proper render', () => {
    // const wrapper = mount(Home, { props: { label: 'Home' } })
    // const labelText = wrapper.find('h1');
    // expect(labelText.text()).toContain('Home')
    expect(true).toBe(true);
  })
})

