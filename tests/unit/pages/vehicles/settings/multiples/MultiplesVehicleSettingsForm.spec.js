import { mount } from '@vue/test-utils';
import MultiplesVehicleSettingsForm from '@/components/pages/vehicles/settings/multiples/MultiplesVehicleSettingsForm.vue';


describe('MultiplesVehicleSettingsForm', () => {
  it('should display proper render', () => {
    // const wrapper = mount(MultiplesVehicleSettingsForm, { props: { label: 'Password' } })
    // const labelText = wrapper.find('label');
    expect(true).toBe(true)
  })
})

