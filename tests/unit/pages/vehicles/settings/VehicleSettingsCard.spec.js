import { mount } from '@vue/test-utils';
import VehicleSettingsCard from '@/components/pages/vehicles/settings/VehicleSettingsCard.vue';


describe('VehicleSettingsCard', () => {
  it('should display proper render', () => {
    // const wrapper = mount(VehicleSettingsCard, { props: { label: 'Password' } })
    // const labelText = wrapper.find('label');
    expect(true).toBe(true)
  })
})

